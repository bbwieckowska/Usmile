% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1_function_USclbr_mdl.R
\name{USclbr_mdl}
\alias{USclbr_mdl}
\title{Calibration of Random Forest predictive probabilities}
\usage{
USclbr_mdl(y, p, method = "isotonic", n_folds = 10, random_seed = 123)
}
\arguments{
\item{y}{Vector of observed binary values (0 or 1)}

\item{p}{Vector of predicted probabilities (values between 0 and 1)}

\item{method}{Calibration method ("isotonic" - default or "logistic")}

\item{n_folds}{Number of folds for cross-validation (default: 10)}

\item{random_seed}{Random seed for reproducibility (default: 123)}
}
\value{
Vector of calibrated probabilities with the same length as the input vector p
}
\description{
This function calibrates predictive probabilities using either isotonic regression
or logistic regression with cross-validation.
}
\examples{
\dontrun{
# Load Heart Disease datasets
data(heart_disease)
data(heart_disease_train)
data(heart_disease_test)

# Train Random Forest model
model_rf <- randomForest::randomForest(disease ~ age + sex, data = train_data)

# Get raw predictions
preds <- predict(model_rf, type = "prob")[, 2]

# Calibrate predictions using isotonic regression
calibrated_preds <- USclbr_mdl(
  y = as.numeric(train_data$disease) - 1,
  p = preds,
  method = "isotonic"
)

# Calibrate predictions using logistic regression with 5-fold CV
calibrated_preds_log <- USclbr_mdl(
  y = as.numeric(train_data$disease) - 1,
  p = preds,
  method = "logistic",
  n_folds = 5
)
}

}
